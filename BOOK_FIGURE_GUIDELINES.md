# 图解规范

图用于缩短读者建立模型的路径，不用于给页面增加装饰。它和正文一样服务于“全局理解为主、关键处进入必要细节”的目标。

## 图应回答一个明确问题

- 一张图只承担一个主要判断，例如“信息经过哪些状态”“两个概念分别负责什么”或“哪个约束造成性能问题”。
- 图题和 alt 文本直接说明该判断；正文仍要完整表达结论，不能把可检索的论证藏在图片里。
- 流程图标出输入、处理、输出和反馈；架构图标出边界与职责；比较图标出比较维度和适用条件。

## 让关系比术语更清楚

- 用位置、连线、箭头和分组表达数据流、控制流、依赖或层级，并在图注说明箭头的含义。
- 不把训练、模型架构和运行时系统画成同一层。参数更新、Attention 计算、KV Cache 与请求调度必须标出各自位置。
- 保留理解结论所需的数值、条件和例外；不堆叠框架名称、实现细节或与图中问题无关的组件。

## 视觉与交付

- 优先使用可缩放 SVG；正文图片居中且能在窄屏阅读。图片中的文字应在 390px 宽度和 PDF 中保持可读。
- 采用现有深绿、暖白、浅桃色和中性灰；颜色只加强结构，不作为唯一信息通道。深色模式下保留足够对比度。
- SVG 必须包含 `<title>` 和 `<desc>`；Markdown 图片要有描述关系的 alt 文本。图中代码、公式和长文本应同时以可选中的正文或代码块给出。
- 图解变更同时检查中文、英文、网页、HTML、EPUB 和 PDF 输出；不要依赖另一仓库的图片路径或远程素材。
