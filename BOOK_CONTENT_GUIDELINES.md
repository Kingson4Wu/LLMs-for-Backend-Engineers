# 书籍内容规范

> 本书的主要目的是让读者清晰地理解原理、从全局理解大模型，并贴近本质地解释清楚；但不应过度陷入大模型的细节。这是一本以全局理解为主、只在部分基础环节进入必要细节的书。

本书面向后端工程师。它帮助读者建立对大模型的整体理解，并在决定理解是否可靠的地方进入必要的机制细节。它不是模型源码、训练配方或应用框架的穷尽手册。后文的全局感、清晰感和底层细节，均应服务于这一定义。

## 三项核心原则

### 先建立全局感

- 每篇文章先说明它在“数学基础—LLM 内部—外部连接—服务运行”中的位置，以及它解决的问题。
- 说明输入、处理、输出和边界；把相邻机制的职责区别讲清楚。
- 将架构、训练过程与运行时系统分开。例如注意力是模型计算，学习率调度属于训练过程，KV Cache 属于推理时的计算复用。

### 用清晰感取代术语堆叠

- 先给可检验的直觉和因果链，再给公式、术语或实现细节。
- 一个类比只解释明确的一面，同时指出不成立的边界；不能用类比替代机制。
- 术语第一次出现时给出简短定义，并链接到读者需要的前置或后续文章。
- 区分典型情况、实现选择和普遍规律；避免“必然”“唯一”“只能”等无条件结论。

### 在关键处抵达本质和细节

- 只有在细节改变读者对因果、限制或工程决策的判断时，才展开公式、推导或实现机制。
- 解释“为什么”时至少覆盖：信号或数据如何流动、哪个约束导致问题、该机制改变了什么、它没有解决什么。
- 保留足以验证关键结论的细节。例如解释缩放点积时说明数值尺度和 Softmax；解释 KV Cache 时说明复用的 Key/Value、仍需计算的 Query 和内存代价。
- 不因追求深入而扩写到内核调优、分布式训练或单一模型家族的专有细节；这类内容只在理解主线所需时作为有边界的延伸阅读。

## 文章写作结构

除非主题确实不适用，每篇文章按以下顺序组织：

1. **定位与问题**：它在全局链路中的位置，以及没有它时的具体限制。
2. **核心机制**：用最少的步骤说明输入如何变化为输出。
3. **必要的底层细节**：公式、短例子或数据流，只解释支撑核心结论的部分。
4. **边界与连接**：它不解决什么、常被混淆的相邻概念，以及下一篇该读什么。
5. **小结**：以可复述的因果关系收束，避免口号式结论。

## 标题、层级与文风

- 目录标题与正文 H1 必须逐字一致；标题采用“概念：读者需要回答的问题”的形式，不保留旧文章式的副标题堆叠、半角中文标点或无关的时间性修饰。
- `##` 只用于文章的主要论证环节，`###` 只用于该环节内确有独立职责的子问题。编号可以帮助推导，但不能替代标题表达的含义；同一篇文章不得混用中文序数、阿拉伯序数和无序标题来表示同一层级。
- 开头直接交代当前问题、它在全书链路中的位置和读者为何需要它；避免“先看”“本章将”“下面我们”一类讲解过程的旁白。
- 段落应先说明具体对象或因果关系，再给公式、术语和抽象结论。不要以“本质上”“核心是”“值得注意的是”等空泛判断代替解释；这些词只有在后面紧接可检验的机制时才使用。
- 列表、表格和图只用于并列关系、流程或比较。连续的论证写成完整段落；小结回到本文的具体结论，不用泛化的鼓励、口号或“希望有所帮助”式收尾。

## Transformer 主题的分层约束

涉及 Transformer 时，明确标出叙述属于哪一层：

| 层次 | 回答的问题 | 典型内容 |
| --- | --- | --- |
| 模型架构 | 表示怎样交换和变换信息 | Attention、FFN、位置机制、Mask、残差与归一化的结构位置 |
| 训练过程 | 参数怎样稳定地学习 | 损失、反向传播、优化器、学习率调度、数值精度 |
| 运行时系统 | 已训练模型怎样执行 | Prefill、Decode、KV Cache、批处理和实现策略 |

同一术语跨层出现时，要说明它们的关系，不能暗示某一层的策略是另一层的定义。例如 FlashAttention 是注意力计算的实现算法，不是注意力机制本身；KV Cache 是因果解码可利用的状态复用，不是长期记忆。

## 事实、来源与双语要求

- 对论文结论、训练经验和系统行为分别陈述，优先链接原始论文、官方文档或可复现的技术资料。
- 保留原理与经验的区别：某个常用配置不等于唯一可行配置。
- 中文是源内容；修改中文后，同步审阅英文镜像的含义、链接、术语和源哈希。
- 章节间只保留一个完整解释的主入口，其他位置用简短说明和链接避免重复。

## 发布前复核清单

- 读者能在开头回答“这解决什么问题、位于哪一层”。
- 每个关键结论都有可追溯的因果解释，且没有把类比写成事实。
- 公式、图示、例子与正文结论一致，条件和例外没有被省略。
- 相邻概念的边界明确，内部链接与中英文镜像都对齐。
- 内容深度服务理解主线，没有滑向实现百科或无关的实践手册。
- 图解的职责、可访问性和交付要求符合 [图解规范](BOOK_FIGURE_GUIDELINES.md)。
